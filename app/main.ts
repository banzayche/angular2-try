// the main file of app. Here we make an import of: bootstrap component (to run app), AppComponent (main component of the app)
import {bootstrap}    from 'angular2/platform/browser'
import {AppComponent} from './app.component'

// starting the app
bootstrap(AppComponent);